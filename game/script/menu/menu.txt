#
# タップメニューのメニュー定義
#

# このファイルの基本設定
global {
    # 下記の画像を使用する
    idle:   system/menu/large-idle.png;
    hover:  system/menu/large-hover.png;
    active: system/menu/large-hover.png;

    # フェードイン時間(秒)
    fadein: 0.1;

    # フェードアウト時間(秒)
    fadeout: 0.1;

    # 余白クリックでキャンセルする
    clickcancel: yes;

    # 余白クリックによるキャンセル時のSE
    cancelse: click.ogg;
}

# セーブ画面へ移るボタン
SAVE {
    # 種類: メニュー移動
    type: menu;

    # 移動先メニューファイル
    file: save.txt;

    # 座標とサイズ
    x: 800;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# セーブ画面へ移るボタン
LOAD {
    # 種類: メニュー移動
    type: menu;

    # 移動先メニューファイル
    file: load.txt;

    # 座標とサイズ
    x: 880;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# オートボタン
AUTO {
    # 種類: オートモード開始
    type: auto;

    # 座標とサイズ
    x: 960;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# スキップボタン
SKIP {
    # 種類: スキップモード開始
    type: skip;

    # 座標とサイズ
    x: 1040;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# ログボタン
HISTORY {
    # 種類: メニュー移動
    type: menu;

    # 移動先メニューファイル
    file: history.txt;

    # 座標とサイズ
    x: 1120;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# コンフィグボタン
CONFIG {
    # 種類: メニュー移動
    type: menu;

    # 移動先メニューファイル
    file: config.txt;

    # 座標とサイズ
    x: 1200;
    y: 0;
    width: 80;
    height: 80;

    # ポイントされたときの効果音
    pointse: button.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}
